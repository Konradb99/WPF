<UserControl x:Class="WPF_Pokedex.Views.MainPanel.MainPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_Pokedex.Views.MainPanel"
             xmlns:pokemons ="clr-namespace:WPF_Pokedex.Views.PokemonList"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type pokemons:PokemonListViewModel}">
            <pokemons:PokemonListView />
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <ContentControl Content="{Binding PokemonCurrentViewModel}" Grid.Column="1" />
    </Grid>
</UserControl>
